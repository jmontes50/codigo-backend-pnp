#Estamos importando solamente una parte de nuestro modulo
#Y podemos añadir un alias de por si a ese import, esto se suele hacer en caso el nombre del modulo no ha dado un nombre claro a la funcion que hemos importado
#as = como
from modulo import despedir as adios

adios("Priscilla")
